# EDMACARS — Descargas APK

Repositorio público **solo para distribución** de la calculadora EDMACARS.

| Versión | Archivo |
|---------|---------|
| **1.0.9** | `edmacars-v1.0.9.apk` |

## URL para OTA (Supabase `apk_url`)

Después de hacer `git push`, usa esta URL directa:

```
https://raw.githubusercontent.com/thewassKIWASS/edmacars-apk-DESCARGAS/main/edmacars-v1.0.9.apk
```

## Supabase `app_config`

- `latest_version` → `1.0.9`
- `apk_url` → la URL de arriba